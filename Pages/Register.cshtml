@page "/admin/auth/register"
@model Quaze.Components.Pages.RegisterModel

<form method="post">
    <input name="@Html.NameFor(x=>x.Username)" placeholder="Username" />
    <input type="password" name="@Html.NameFor(x=>x.Password)" placeholder="Username" />
    <button type="submit">Register</button>
</form>
